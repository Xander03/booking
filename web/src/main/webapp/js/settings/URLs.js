const ROOT = '/view';

const SIGN = '/sign';
const HOTEL = '/hotel';
const ROOM = "/room";

export const HOME = ROOT;
export const FORBIDDEN = ROOT + '/forbidden';

export const SIGN_IN = ROOT + SIGN + '/sign_in';
export const SIGN_UP = ROOT + SIGN + '/sign_up';

export const HOTEL_FORM = ROOT + HOTEL + '/form';
export const HOTEL_TARGET = ROOT + HOTEL + '/target';
export const HOTEL_LIST = ROOT + HOTEL + '/list';

export const ROOM_FORM = ROOT + ROOM + '/form';
export const ROOM_TARGET = ROOT + ROOM + '/target';
